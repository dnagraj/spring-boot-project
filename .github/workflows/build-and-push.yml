name: Build Scan and Push

on:
  push:
    branches:
      - main

  workflow_dispatch:

permissions:
  contents: read
  id-token: write

env:
  AWS_REGION: us-east-1
  ECR_REPOSITORY: demo-app

jobs:
  build-test-scan-push:
    runs-on: ubuntu-latest

    steps:

      # ----------------------------------------------------
      # 1. Checkout source
      # ----------------------------------------------------

      - name: Checkout source
        uses: actions/checkout@v4

      # ----------------------------------------------------
      # 2. Setup Java 25
      # ----------------------------------------------------

      - name: Setup Java 25
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '25'
          cache: maven
          cache-dependency-path: demo-app/pom.xml

      # ----------------------------------------------------
      # 3. Build and test Spring Boot application
      # ----------------------------------------------------

      - name: Build and test
        working-directory: demo-app
        run: |
          chmod +x mvnw
          ./mvnw clean verify

      # ----------------------------------------------------
      # 4. Debug GitHub context
      # ----------------------------------------------------

      - name: Verify GitHub context
        run: |
          echo "Repository: $GITHUB_REPOSITORY"
          echo "Ref: $GITHUB_REF"
          echo "Branch: $GITHUB_REF_NAME"
          echo "Event: $GITHUB_EVENT_NAME"
            - name: Verify AWS role configuration
        env:
          AWS_ROLE_ARN: ${{ secrets.AWS_GITHUB_ACTIONS_ROLE_ARN }}
          run: |
            echo "AWS role configured: ${AWS_ROLE_ARN:0:25}..."
            echo "Role account: ${AWS_ROLE_ARN:13:12}"
      # ----------------------------------------------------
      # 5. Configure AWS using GitHub OIDC
      # ----------------------------------------------------

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_GITHUB_ACTIONS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      # ----------------------------------------------------
      # 6. Verify AWS identity
      # ----------------------------------------------------

      - name: Verify AWS identity
        run: |
          aws sts get-caller-identity

      # ----------------------------------------------------
      # 7. Login to Amazon ECR
      # ----------------------------------------------------

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      # ----------------------------------------------------
      # 8. Build Docker image
      # ----------------------------------------------------

      - name: Build Docker image
        env:
          REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build \
            -t "$REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" \
            ./demo-app

      # ----------------------------------------------------
      # 9. Scan Docker image with Trivy
      # ----------------------------------------------------

      - name: Scan Docker image with Trivy
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: '${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_REPOSITORY }}:${{ github.sha }}'
          format: table
          exit-code: '1'
          ignore-unfixed: true
          severity: CRITICAL,HIGH

      # ----------------------------------------------------
      # 10. Push image to ECR
      # ----------------------------------------------------

      - name: Push Docker image
        env:
          REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker push "$REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG"